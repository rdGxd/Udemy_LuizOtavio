// (condição)  ? "Valor verdeiro" : "Valor falso"
const pontuacaoUsuer = 1000;
const nivelUser = pontuacaoUsuer >= 1000 ? "VIP" : "Normal";

const corUsuario = null;
const corPadrao = corUsuario || "Preta";
console.log(nivelUsuario, corPadrao)
